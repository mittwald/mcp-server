# Work Packages: MCP Functional Test Suite

**Inputs**: Design documents from `/kitty-specs/005-mcp-functional-test/`
**Prerequisites**: plan.md (required), spec.md (7 user stories), research.md (10 test domains), data-model.md, contracts/harness-api.ts, quickstart.md

**Tests**: Testing deferred to validation via actual MCP server execution. No unit tests specified.

**Organization**: Fine-grained subtasks (`Txxx`) roll up into work packages (`WPxx`). Each work package must be independently deliverable and testable.

**Prompt Files**: Each work package references a matching prompt file in `tasks/planned/` generated by `/spec-kitty.tasks`. Treat this file as the high-level checklist; keep deep implementation detail inside the prompt files.

## Subtask Format: `[Txxx] [P?] Description`
- **[P]** indicates the subtask can proceed in parallel (different files/components).
- Include precise file paths or modules.

## Path Conventions
- **Source code**: `tests/functional/src/`
- **Configuration**: `tests/functional/config/`
- **Output**: `tests/functional/output/`

---

## Work Package WP01: Project Setup & Core Types (Priority: P0)

**Goal**: Establish project skeleton, dependencies, and shared type definitions.
**Independent Test**: `npm install` succeeds; TypeScript compiles with no errors.
**Prompt**: `tasks/planned/WP01-project-setup.md`

### Included Subtasks
- [X] T001 Create `tests/functional/` directory structure per plan.md
- [X] T002 Initialize `package.json` with TypeScript, @anthropic-ai/sdk dependencies
- [X] T003 [P] Configure `tsconfig.json` for Node.js 20+ ES modules
- [X] T004 [P] Create `tests/functional/src/types/index.ts` with all shared types from data-model.md
- [X] T005 [P] Create `tests/functional/config/mcp-server.json` configuration template
- [X] T006 Create main entry point `tests/functional/src/harness/index.ts` (stub)

### Implementation Notes
- Use ES modules (type: "module" in package.json)
- Types must match `contracts/harness-api.ts` and `data-model.md` exactly
- Configure output directories: `output/`, `output/sessions/`

### Parallel Opportunities
- T003, T004, T005 can proceed in parallel once T001/T002 complete.

### Dependencies
- None (starting package).

### Risks & Mitigations
- Type drift between spec and implementation; use strict TypeScript.

---

## Work Package WP02: Session Runner - Claude Code Spawning (Priority: P0)

**Goal**: Spawn Claude Code headless sessions with streaming JSON output and tool restrictions.
**Independent Test**: Single test prompt executes, session_id captured, `mw` CLI blocked.
**Prompt**: `tasks/planned/WP02-session-runner.md`

### Included Subtasks
- [X] T007 Implement `session-runner.ts` - spawn Claude Code subprocess (FR-001)
- [X] T008 Add `--disallowedTools "Bash(mw)"` enforcement (FR-002)
- [X] T009 Implement streaming JSON line parsing from stdout (FR-004)
- [X] T010 Extract session_id from `stream-json` result events
- [X] T011 Implement session timeout and kill capability
- [X] T012 Handle subprocess errors and exit codes
- [X] T013 Create `SpawnSessionOptions` and `SessionResult` implementation per contracts

### Implementation Notes
- Use `child_process.spawn()` with `{ stdio: ['pipe', 'pipe', 'pipe'] }`
- Parse streaming JSONL format from `--output-format stream-json`
- Command: `claude -p "<prompt>" --output-format stream-json --model claude-3-haiku-20240307 --disallowedTools "Bash(mw)" --mcp-config <path>`

### Parallel Opportunities
- T007-T013 are sequential (building the session runner incrementally).

### Dependencies
- Depends on WP01 (types).

### Risks & Mitigations
- Claude CLI API changes; pin Claude Code version in docs.

---

## Work Package WP03: Stream Parser & Haiku Coordinator (Priority: P0)

**Goal**: Parse streaming output and implement Haiku-powered meta-agent for stuck detection.
**Independent Test**: Coordinator correctly identifies stuck patterns (>3 errors, >60s idle, >5 repeats).
**Prompt**: `tasks/planned/WP03-stream-parser-coordinator.md`

### Included Subtasks
- [X] T014 Implement `stream-parser.ts` - parse stream-json events (message, tool_use, tool_result, error, result)
- [X] T015 Extract tool call patterns from streaming events
- [X] T016 Track consecutive errors, idle time, repeated tool calls
- [X] T017 Implement `coordinator.ts` - Haiku meta-agent interface
- [X] T018 Define coordinator analysis prompt template
- [X] T019 Implement intervention triggers (FR-005a): >3 errors, >60s idle, >5 repeats
- [X] T020 Add coordinator decision logging

### Implementation Notes
- Use `@anthropic-ai/sdk` for Haiku calls
- Coordinator receives CoordinatorInput, returns CoordinatorDecision
- Log all coordinator decisions to `output/coordinator.log`

### Parallel Opportunities
- T014-T16 (stream parser) can parallel with T17-T20 (coordinator) once interfaces defined.

### Dependencies
- Depends on WP02 (session runner emits stream events).

### Risks & Mitigations
- Haiku API latency; batch analysis calls, don't call per-event.

---

## Work Package WP04: Manifest & Coverage Tracking (Priority: P1)

**Goal**: Implement append-only JSONL manifest and coverage statistics.
**Independent Test**: Multiple concurrent appends maintain data integrity; coverage query returns accurate stats.
**Prompt**: `tasks/planned/WP04-manifest-coverage.md`

### Included Subtasks
- [X] T021 Implement `manifest.ts` - atomic JSONL append (FR-005)
- [X] T022 Create ManifestEntry serialization per data-model.md
- [X] T023 Implement getCoverage() - parse manifest, compute statistics (FR-020)
- [X] T024 Implement getToolHistory() - query entries by tool name
- [X] T025 [P] Create coverage CLI command (`npm run coverage`)
- [X] T026 Handle concurrent append safety (atomic write per line)

### Implementation Notes
- Use `fs.appendFileSync()` with exclusive flag for atomicity
- ManifestEntry format: `{"toolName":"...", "sessionId":"...", "status":"...", "timestamp":"...", ...}`
- Coverage calculation: count unique tools with status != untested

### Parallel Opportunities
- T025 (coverage CLI) can proceed once T21-T24 complete.

### Dependencies
- Depends on WP01 (types).

### Risks & Mitigations
- Concurrent corruption; test with parallel writes before integration.

---

## Work Package WP05: Resource Management & Cleanup (Priority: P1)

**Goal**: Track created resources and implement domain-grouped cleanup.
**Independent Test**: Resources tracked correctly; cleanup removes all tracked resources without orphans.
**Prompt**: `tasks/planned/WP05-resource-management.md`

### Included Subtasks
- [X] T027 Implement `tracker.ts` - resource creation tracking (FR-008)
- [X] T028 Implement `naming.ts` - collision-safe naming convention (FR-010)
- [X] T029 Implement `cleanup.ts` - domain-grouped cleanup (FR-009)
- [X] T030 Add dependency ordering for cleanup (children before parents)
- [X] T031 [P] Implement `/tmp` artifact tracking (FR-021)
- [X] T032 [P] Implement `/tmp` artifact cleanup (FR-022)
- [X] T033 Add orphan detection using naming convention `test-{domain}-*`
- [X] T034 Implement clean-room vs harness-assisted mode selection (FR-006, FR-007)

### Implementation Notes
- Naming: `test-{domain}-{timestamp}-{random4}`
- Cleanup order: mail → virtualhosts → apps → databases → volumes → projects
- Store tracker state in `output/resources.json`

### Parallel Opportunities
- T031-T32 (/tmp) can parallel with T027-T30 (Mittwald resources).

### Dependencies
- Depends on WP01 (types).

### Risks & Mitigations
- Cleanup failures leave orphans; implement retry with logging.

---

## Work Package WP06: Tool Inventory & Discovery (Priority: P1)

**Goal**: Dynamically discover MCP tools and categorize by test domain.
**Independent Test**: Discovery returns ~174 tools; tools correctly grouped into 10 domains.
**Prompt**: `tasks/planned/WP06-tool-inventory.md`

### Included Subtasks
- [X] T035 Implement `discovery.ts` - MCP server tool discovery
- [X] T036 Parse tool names and descriptions from MCP response
- [X] T037 Implement `grouping.ts` - map tools to 10 test domains per research.md
- [X] T038 Generate `test-domains.json` configuration from discovery
- [X] T039 Implement tool tier assignment (0-4 dependency levels)
- [X] T040 Create `tool-manifest.ts` - ToolEntry storage and lookup
- [X] T041 Implement getByDomain() and getByTier() queries

### Implementation Notes
- Connect to MCP server at `https://mittwald-mcp-fly2.fly.dev/mcp`
- Domain mapping based on tool name prefix: `app/*` → apps, `database/*` → databases
- Tier 0: user/*, login/*, context/*, org/list, server/list
- Tier 3: project/create (clean-room)
- Tier 4: Everything requiring project context

### Parallel Opportunities
- T037-T41 can parallel once T035-T36 provide discovered tools.

### Dependencies
- Depends on WP01 (types).

### Risks & Mitigations
- MCP server changes tool list; discovery runs at harness startup.

---

## Work Package WP07: Harness Orchestration (Priority: P1) 🎯 MVP

**Goal**: Orchestrate parallel test execution with concurrency control and polling-based completion.
**Independent Test**: Run 3-5 tests concurrently; all complete without resource conflicts.
**Prompt**: `tasks/planned/WP07-harness-orchestration.md`

### Included Subtasks
- [X] T042 Implement main `harness/index.ts` orchestration loop
- [X] T043 Add test queue management with priority ordering
- [X] T044 Implement concurrency control (3-5 concurrent sessions) (FR-003)
- [X] T045 Integrate session runner, stream parser, coordinator
- [X] T046 Add polling-based completion detection (30s intervals) (FR-013)
- [X] T047 Implement partial result recording on failure (FR-013a)
- [X] T048 Add rate limit handling with exponential backoff (FR-013b)
- [X] T049 Add eventual consistency retry logic (FR-013c)
- [X] T050 Wire manifest appends after session completion
- [X] T051 Wire resource cleanup after domain completion

### Implementation Notes
- Queue priority: Tier 0 → Tier 1 → Tier 3 (project/create) → Tier 4
- Session pool with semaphore for concurrency control
- After each session: append manifest, update coverage
- After domain completes: run grouped cleanup

### Parallel Opportunities
- T042-T51 are sequential (building the orchestrator incrementally).

### Dependencies
- Depends on WP02, WP03, WP04, WP05, WP06 (all components).

### Risks & Mitigations
- Complex integration; build incrementally with smoke tests.

---

## Work Package WP08: CLI & Session Preservation (Priority: P2)

**Goal**: Complete CLI interface and session log preservation for future analysis.
**Independent Test**: All CLI commands work; session logs retrievable by session_id.
**Prompt**: `tasks/planned/WP08-cli-session-preservation.md`

### Included Subtasks
- [X] T052 Create CLI entry point with command parsing
- [X] T053 Implement `npm run test:all` - full test suite
- [X] T054 Implement `npm run test:domain -- --domain <name>` - single domain
- [X] T055 Implement `npm run test:tool -- --tool <name>` - single tool
- [X] T056 Implement `npm run cleanup -- --domain <name>` - manual cleanup
- [X] T057 Implement `npm run status` - current harness status
- [X] T058 Implement session log preservation configuration (FR-011)
- [X] T059 Create session → log file mapping storage (FR-012, FR-015)
- [X] T060 Implement `npm run list-resources -- --orphaned` - orphan detection
- [X] T061 Add quickstart.md validation scenario execution

### Implementation Notes
- CLI uses commander.js or yargs
- Session mapping: `output/sessions/{session_id}.json` → log path
- Configure `~/.claude/settings.json` with `cleanupPeriodDays: 99999`

### Parallel Opportunities
- T052-T57 (CLI) can parallel with T058-T59 (session preservation).

### Dependencies
- Depends on WP07 (harness orchestration).

### Risks & Mitigations
- Claude Code settings location may vary; document override options.

---

## Dependency & Execution Summary

- **Sequence**: WP01 → WP02 → WP03 → (WP04, WP05, WP06 parallel) → WP07 → WP08
- **Parallelization**: WP04, WP05, WP06 can proceed in parallel after WP03 completes.
- **MVP Scope**: WP01-WP07 constitute the minimal working harness; WP08 adds polish.

```
WP01 (Setup)
    ↓
WP02 (Session Runner)
    ↓
WP03 (Stream Parser + Coordinator)
    ↓
    ├─→ WP04 (Manifest) ───┐
    ├─→ WP05 (Resources) ──┼─→ WP07 (Orchestration) → WP08 (CLI)
    └─→ WP06 (Inventory) ──┘
```

---

## Subtask Index (Reference)

| Subtask ID | Summary | Work Package | Priority | Parallel? |
|------------|---------|--------------|----------|-----------|
| T001 | Create directory structure | WP01 | P0 | No |
| T002 | Initialize package.json | WP01 | P0 | No |
| T003 | Configure tsconfig.json | WP01 | P0 | Yes |
| T004 | Create shared types | WP01 | P0 | Yes |
| T005 | Create mcp-server.json template | WP01 | P0 | Yes |
| T006 | Create harness stub | WP01 | P0 | No |
| T007 | Implement session-runner spawn | WP02 | P0 | No |
| T008 | Add disallowedTools enforcement | WP02 | P0 | No |
| T009 | Implement streaming JSON parsing | WP02 | P0 | No |
| T010 | Extract session_id | WP02 | P0 | No |
| T011 | Implement timeout/kill | WP02 | P0 | No |
| T012 | Handle subprocess errors | WP02 | P0 | No |
| T013 | Create SpawnSessionOptions impl | WP02 | P0 | No |
| T014 | Implement stream-parser | WP03 | P0 | No |
| T015 | Extract tool call patterns | WP03 | P0 | No |
| T016 | Track error/idle/repeat patterns | WP03 | P0 | No |
| T017 | Implement coordinator interface | WP03 | P0 | Yes |
| T018 | Define coordinator prompt | WP03 | P0 | Yes |
| T019 | Implement intervention triggers | WP03 | P0 | No |
| T020 | Add coordinator logging | WP03 | P0 | No |
| T021 | Implement manifest append | WP04 | P1 | No |
| T022 | Create ManifestEntry serialization | WP04 | P1 | No |
| T023 | Implement getCoverage() | WP04 | P1 | No |
| T024 | Implement getToolHistory() | WP04 | P1 | No |
| T025 | Create coverage CLI command | WP04 | P1 | Yes |
| T026 | Handle concurrent append safety | WP04 | P1 | No |
| T027 | Implement resource tracker | WP05 | P1 | No |
| T028 | Implement naming convention | WP05 | P1 | No |
| T029 | Implement domain cleanup | WP05 | P1 | No |
| T030 | Add dependency ordering | WP05 | P1 | No |
| T031 | Implement /tmp tracking | WP05 | P1 | Yes |
| T032 | Implement /tmp cleanup | WP05 | P1 | Yes |
| T033 | Add orphan detection | WP05 | P1 | No |
| T034 | Implement clean-room mode | WP05 | P1 | No |
| T035 | Implement MCP discovery | WP06 | P1 | No |
| T036 | Parse tool metadata | WP06 | P1 | No |
| T037 | Implement domain grouping | WP06 | P1 | Yes |
| T038 | Generate test-domains.json | WP06 | P1 | Yes |
| T039 | Implement tier assignment | WP06 | P1 | Yes |
| T040 | Create tool-manifest storage | WP06 | P1 | No |
| T041 | Implement domain/tier queries | WP06 | P1 | No |
| T042 | Implement orchestration loop | WP07 | P1 | No |
| T043 | Add queue management | WP07 | P1 | No |
| T044 | Implement concurrency control | WP07 | P1 | No |
| T045 | Integrate components | WP07 | P1 | No |
| T046 | Add polling completion detection | WP07 | P1 | No |
| T047 | Implement partial result recording | WP07 | P1 | No |
| T048 | Add rate limit handling | WP07 | P1 | No |
| T049 | Add eventual consistency retry | WP07 | P1 | No |
| T050 | Wire manifest appends | WP07 | P1 | No |
| T051 | Wire cleanup after domain | WP07 | P1 | No |
| T052 | Create CLI entry point | WP08 | P2 | No |
| T053 | Implement test:all command | WP08 | P2 | No |
| T054 | Implement test:domain command | WP08 | P2 | No |
| T055 | Implement test:tool command | WP08 | P2 | No |
| T056 | Implement cleanup command | WP08 | P2 | No |
| T057 | Implement status command | WP08 | P2 | No |
| T058 | Implement log retention config | WP08 | P2 | Yes |
| T059 | Create session mapping storage | WP08 | P2 | Yes |
| T060 | Implement orphan listing | WP08 | P2 | No |
| T061 | Validate quickstart scenario | WP08 | P2 | No |

---

> Total: 61 subtasks across 8 work packages. MVP = WP01-WP07 (51 subtasks).
