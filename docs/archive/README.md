# Archived Documentation

Legacy analyses retained for historical context. These documents describe the pre-bridge `oidc-provider` deployment, early MCP Jam investigations, and other superseded workflows. Use them only for forensic debugging—current architecture guidance lives in `docs/INDEX.md`.
