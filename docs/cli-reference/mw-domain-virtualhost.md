# mw domain virtualhost

```
$ mw domain virtualhost
Create a new ingress

USAGE
  $ mw domain virtualhost COMMAND

COMMANDS
  domain virtualhost create  Create a new ingress
  domain virtualhost delete  Delete a virtual host
  domain virtualhost get     Get a virtual host.
  domain virtualhost list    List virtualhosts for a project.

```
