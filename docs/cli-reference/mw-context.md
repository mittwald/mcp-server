# mw context

```
$ mw context
Save certain environment parameters for later use

USAGE
  $ mw context COMMAND

COMMANDS
  context get    Print an overview of currently set context parameters
  context reset  Reset context values
  context set    Set context values for the current project, org or server

```
