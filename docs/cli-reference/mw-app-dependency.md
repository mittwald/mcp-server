# mw app dependency

```
$ mw app dependency
Manage the system dependencies of your apps

USAGE
  $ mw app dependency COMMAND

COMMANDS
  app dependency list      Get all available dependencies
  app dependency update    Update the dependencies of an app
  app dependency versions  Get all available versions of a particular dependency

```
