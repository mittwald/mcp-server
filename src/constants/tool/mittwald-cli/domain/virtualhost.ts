import type { Tool } from '@modelcontextprotocol/sdk/types.js';

export const mittwald_domain_virtualhost: Tool = {
  name: "mittwald_domain_virtualhost",
  description: "Get information about available virtual host commands.",
  inputSchema: {
    type: "object",
    properties: {},
    required: []
  }
};