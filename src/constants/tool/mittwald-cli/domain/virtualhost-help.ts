import type { Tool } from '@modelcontextprotocol/sdk/types.js';

export const mittwald_domain_virtualhost_help: Tool = {
  name: "mittwald_domain_virtualhost_help",
  description: "Show help for virtual host (ingress) commands",
  inputSchema: {
    type: "object",
    properties: {},
    required: []
  }
};